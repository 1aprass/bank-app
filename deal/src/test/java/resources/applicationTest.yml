spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/deal-test
    username: ${DbTest_USERNAME:postgres}
    password: ${DbTest_PASSWORD:483711}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  liquibase:
    enabled: false
