package ru.neoflex.calculator.util;

import java.math.BigDecimal;

public class CalculatorUtils {

    public static BigDecimal calculateMonthlyPayment(BigDecimal amount, BigDecimal rate, int term) {
        return new BigDecimal("0");
    }
    //todo реализация класса подсчета ежемесячного платежа
}